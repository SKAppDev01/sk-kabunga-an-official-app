ROLLBACK: Home modernization

This restores the UI state from immediately before home-modernized.zip.

Run in Termux:

cd ~/sk-kabunga-an-official-app
unzip -o ~/storage/downloads/home-modernization-rollback.zip
npx tsc --noEmit

If TypeScript is clean:

npx expo start --dev-client -c

Restored files:
- src/app/(tabs)/home.tsx
- src/app/upcoming-events.tsx
- src/app/activity-history.tsx
